local World = require "entropy.world"
local Vec3 = require "entropy.math.vec3"

local Camera: type = @record{
  pos: Vec3,
  rot: Vec3,

  fov: float32,

  z_near: float32,
  z_far: float32,
}

-- register storage for Camera
World.r("entropy.render.scene.cameras", Camera)

return Camera
